# K3b 刻录软件（中文版）详细说明

> 界面与 KDE 的 **K3b** 类似的 CD/DVD/BD 刻录工具，完全中文化。
> 底层调用 Linux 系统已有的刻录工具，C++ / Qt6 编写。
>
> 该项目为演示与学习用途的中文实现，与 KDE 官方 K3b 项目无关。

---

## 目录

1. [项目概述](#1-项目概述)
2. [功能特性](#2-功能特性)
3. [界面与操作说明](#3-界面与操作说明)
4. [编译指南](#4-编译指南)
5. [安装方法](#5-安装方法)
6. [使用场景指南](#6-使用场景指南)
7. [底层工具依赖](#7-底层工具依赖)
8. [技术架构](#8-技术架构)
9. [已知问题与修复](#9-已知问题与修复)
10. [故障排查](#10-故障排查)
11. [命令行自检](#11-命令行自检)
12. [版本历史](#12-版本历史)
13. [常见问题 FAQ](#13-常见问题-faq)

---

## 1. 项目概述

**K3b 刻录软件（中文版）**是一个面向 Linux 桌面用户的图形化光盘刻录工具。它复刻了 KDE 经典刻录软件 K3b 的界面布局与核心工作流（欢迎页 → 项目页 → 烧录进度），但完全重新实现，界面全部使用简体中文。

**设计原则：**

- **不重复造轮子**：刻录本身调用系统成熟工具（wodim、growisofs、genisoimage 等），程序负责界面、项目组织、参数编排、输出解析与状态展示。
- **工具缺失不崩**：检测不到光驱或刻录工具时自动进入**演示模式**，模拟进度与日志，界面功能完整可体验。
- **开箱即用**：编译产物单二进制，无资源文件依赖（图标均为程序内绘制）。

---

## 2. 功能特性

| 功能 | 说明 | 依赖工具 |
|------|------|----------|
| **数据光盘项目** | 拖拽添加文件/文件夹，卷标设置，容量条实时显示，一键烧录 | genisoimage + wodim / growisofs |
| **音乐 CD 项目** | 添加 WAV/MP3/OGG/FLAC 音轨，自动转码为 44.1kHz/16bit WAV 后刻录 | ffmpeg + wodim |
| **光盘复制** | 数据盘 `dd` 整盘读取后烧录；音频盘 `cdrdao` 精确复制（含 TOC） | dd / cdrdao |
| **ISO 镜像工具** | 制作 ISO（RockRidge + Joliet + iso-level 3）与烧录镜像 | genisoimage / growisofs / wodim |
| **介质信息** | 检测介质类型、空白/可写/可擦除/可续刻状态、真实容量、厂商型号 | dvd+rw-mediainfo / wodim |
| **擦除光盘** | 快速/完全擦除 CD-RW、DVD±RW；强制格式化 DVD+RW / DVD-RAM / BD-RE | wodim / dvd+rw-format |
| **刻录进度与日志** | 实时进度条（解析 growisofs/wodim/cdrdao/dd 输出）+ 时间戳日志 + 可取消 | — |
| **演示模式** | 无光驱/无工具时模拟烧录进度与日志 | — |
| **项目保存/打开** | 数据/音乐项目存为 `.k3bcn` 文件，随时继续 | — |

**特色细节：**

- **8cm 迷你盘精确容量**：从 ATIP lead-out 扇区数计算真实容量（如 219 MB / 25:02），数据容量条与音乐时长限制均按真实值。
- **强制低速刻录**：所有速度下拉框提供「自动 / 1x / 4x」预置项（光驱硬件不支持时自动忽略，如部分光驱锁定最低 10x）。
- **老光驱兼容**：数据与音频统一使用 TAO 模式（规避 DAO CUE sheet 被老光驱拒收的问题）。
- **wodim 缓冲区问题规避**：所有 wodim 调用显式传 `fs=6000`（见[第 9 节](#9-已知问题与修复)）。

---

## 3. 界面与操作说明

### 3.1 主窗口

```
┌─────────────────────────────────────────────────────────────┐
│ 菜单栏: 文件 编辑 工具 设置 帮助                              │
├─────────────────────────────────────────────────────────────┤
│ 工具栏: [新建数据][新建音乐] | [打开][保存] | [开始烧录] | [弹出] │
├──────────┬──────────────────────────────────────────────────┤
│ 左侧导航  │  中央内容区（欢迎页 / 各功能页 / 烧录进度页）        │
│ 数据项目  │                                                  │
│ 音乐 CD   │                                                  │
│ 复制光盘  │                                                  │
│ ISO 工具  │                                                  │
│ 介质信息  │                                                  │
│ 擦除光盘  │                                                  │
│ 刻录进度  │                                                  │
├──────────┴──────────────────────────────────────────────────┤
│ 状态栏: 设备:/dev/sr0 厂商型号 │ 介质:空白 CD-R（219 MB）│ 工具状态 │
└─────────────────────────────────────────────────────────────┘
```

- **左侧导航**：点击切换功能页；「刻录进度」页在烧录开始时自动跳转。
- **状态栏**：左侧显示设备与驱动型号，中间显示当前介质状态（空白/已用/只读 + 容量），右侧显示工具就绪或演示模式警告。
- **弹出按钮**：弹出光驱托盘（便携光驱弹出后需手动推回）。

### 3.2 欢迎页

K3b 风格的项目卡片，点击进入对应功能：

| 卡片 | 说明 |
|------|------|
| 新建数据光盘项目 | 进入数据项目页 |
| 新建音乐 CD 项目 | 进入音乐 CD 项目页 |
| 复制光盘 | 进入复制页 |
| ISO 镜像工具 | 进入 ISO 工具页 |
| 介质信息 | 进入介质信息页 |
| 擦除可重写光盘 | 进入擦除页 |

### 3.3 数据项目页

```
[添加文件…] [添加文件夹…] [移除选中] [清空]   卷标:[MY_DATA] 写入速度:[自动]   [开始烧录]
┌──────────────────────────────────────────────────────────────┐
│ 名称        │ 大小    │ 类型      │ 路径                       │
│ 报告.docx   │ 1.2 MiB │ DOCX 文件 │ /home/.../报告.docx        │
│ 照片/       │ 89 MiB  │ 文件夹    │ /home/.../照片             │
└──────────────────────────────────────────────────────────────┘
项目大小：90.2 MiB（2 个条目）  ████████████░░░░░░  90.2 MiB / 219 MB
```

- **添加文件/文件夹**：文件选择对话框；也支持**从文件管理器直接拖拽**到列表。
- **去重**：同一路径不会重复添加（按绝对路径判断）。
- **卷标**：光盘卷标，≤16 字符（建议用 ASCII，兼容性最好）。
- **容量条**：项目总大小 vs 介质真实容量；**超出容量变红并警告**。
- **开始烧录**：弹出确认（光盘非空白时二次确认）→ 自动跳转进度页。
  - CD：先生成临时 ISO 再以 TAO 写入；DVD/BD：growisofs 直接写入。
  - 多会话在「设置 → 刻录设置」中开启。

### 3.4 音乐 CD 项目页

```
[添加音轨…] [移除选中] [清空]   写入速度:[自动]   [刻录音乐 CD]
┌──────────────────────────────────────────────────────────────┐
│ 轨道    │ 标题            │ 时长   │ 格式 │ 大小             │
│ 轨道 01 │ 妖精といた夏     │ 06:11  │ WAV  │ 62.4 MiB        │
└──────────────────────────────────────────────────────────────┘
总时长：06:11（1 个音轨）                  CD 剩余容量：18:51 / 25:02
```

- **音轨时长探测**：WAV 直接解析 RIFF 头；其他格式尝试 ffprobe；都没有则按 128kbps 估算。
- **时长限制**：总时长按介质真实音频容量（扇区数 ÷ 75）计算，迷你盘 25:02、标准盘 80 分钟；超出时红色警告并二次确认。
- **刻录**：非 WAV 音轨先用 ffmpeg 逐条转码（44.1kHz/16bit/立体声），再以 TAO 模式刻录（音轨间标准 2 秒间隙）。

### 3.5 复制光盘页

```
源设备:[/dev/sr0]  目标设备:[/dev/sr0]  复制方式:[自动检测]
写入速度:[自动]  [x] 刻录完成后弹出光盘
                                            [开始复制]
```

- **自动检测**：数据盘用 `dd` 整盘读取 → 烧录；音频盘需手动选「仅音频盘」（cdrdao 精确复制）。
- 源盘与目标盘为同一设备时，读取完成后会提示换盘。
- 临时镜像保存在设置中指定的临时目录。

### 3.6 ISO 镜像工具页

两个标签页：

- **制作镜像**：源文件/文件夹列表 + 卷标 + 输出路径 → `genisoimage -r -J -joliet-long -iso-level 3 -V <卷标> -o <输出> <源...>`
- **烧录镜像**：选择 .iso + 目标设备 + 速度 → 按介质类型自动选择 growisofs（DVD/BD）或 wodim（CD）。

### 3.7 介质信息页

```
设备:[/dev/sr0 ▼]  [重新检测] [弹出光盘]
属性            │ 值
设备            │ /dev/sr0
厂商            │ （空白）
型号            │ DVD RW
介质类型        │ CD-R（CD-R）
是否可写        │ 是
是否空白        │ 是
可续刻          │ 否
可擦除          │ 否
容量            │ 219 MB（230,400,000 字节）
```

检测逻辑（依次尝试，取第一个成功结果）：

1. `dvd+rw-mediainfo <设备>`：DVD/BD 介质最可靠（Mounted Media、Disc Status、Erasable、容量）。
2. `wodim dev=<设备> -atip`：CD 介质（可擦除标志区分 CD-R/CD-RW；ATIP lead-out 计算真实容量）。
3. `wodim -msinfo` + `wodim -toc`：判断空白（无会话且无轨道 = 空白；音频盘有轨道 ≠ 空白）。

### 3.8 擦除光盘页

- **快速擦除**（数秒）/ **完全擦除**（彻底）/ **强制重新格式化**（DVD+RW / BD-RE）。
- 执行前强确认对话框。仅可擦除介质（CD-RW、DVD±RW 等）有效；CD-R 等一次性介质无法擦除。

### 3.9 烧录进度页

```
正在刻录数据光盘：我的项目          [取消刻录] [返回主界面]
████████████████████░░░░  62%
正在写入… 62%
[16:16:37] Track 01:    0 of   62 MB written.
[16:16:38] Track 01:    1 of   62 MB written [buf 100%]   5.0x.
...
```

- 进度条解析：growisofs `( 27.2%)`、wodim `22% written`、cdrdao 百分比、dd 字节数。
- 无进度输出的阶段（如 ISO 制作）显示不定进度（动画）。
- **取消**：向刻录进程发终止信号。注意：TAO 烧录中途取消会留下不可用的盘（CD-R 无法续写）。

### 3.10 刻录设置对话框（设置 → 刻录设置）

| 设置项 | 说明 |
|--------|------|
| 默认刻录设备 | 光驱设备路径 |
| 写入速度 | 自动 / 1x / 4x / 检测到的速度 |
| 写入模式 | 自动 / DAO / TAO / RAW（当前刻录路径统一 TAO） |
| 刻录完成后弹出 | 默认开启 |
| 刻录完成后校验 | 预留（当前版本未执行校验） |
| 允许多会话刻录 | 数据盘开启后可用 `-multi` 追加会话 |
| 临时镜像目录 | 生成 ISO/复制镜像的存放位置 |

---

## 4. 编译指南

### 4.1 依赖

- CMake ≥ 3.16
- 编译器：g++ / clang++（C++17）
- Qt6（Widgets 模块，开发包）

**Ubuntu / Debian：**

```bash
sudo apt-get update
sudo apt-get install -y cmake qt6-base-dev
```

### 4.2 编译

```bash
cd k3b-cn
cmake -S . -B build
cmake --build build -j$(nproc)
```

产物：`build/k3b-cn`

### 4.3 运行

```bash
./build/k3b-cn                       # 图形界面
./build/k3b-cn --check-media         # 命令行自检（详见第 11 节）
```

### 4.4 应用菜单集成

```bash
# 菜单项 + 图标（用户级，无需 root）
mkdir -p ~/.local/share/applications ~/.local/share/icons
cp build/k3b-cn ~/.local/bin/ 2>/dev/null || sudo cp build/k3b-cn /usr/local/bin/
# 然后创建 desktop 文件（可参考仓库根目录的打包脚本）
```

---

## 5. 安装方法

### 5.1 deb 包（Debian / Ubuntu）

```bash
sudo dpkg -i k3b-cn_1.0.0_amd64.deb
sudo apt -f install        # 如提示缺失依赖
```

自动安装菜单项与图标；依赖声明 `libqt6widgets6` 等；推荐安装刻录工具。

### 5.2 tar.gz（通用 Linux）

```bash
tar xzf k3b-cn-1.0.0-linux-x86_64.tar.gz
cd k3b-cn-1.0.0-linux-x86_64
sudo cp k3b-cn /usr/local/bin/
# 手动创建菜单项或直接运行 /usr/local/bin/k3b-cn
```

### 5.3 刻录工具安装（强烈建议）

```bash
sudo apt-get install -y wodim genisoimage growisofs cdrdao dvd+rw-tools ffmpeg eject
```

缺少工具时程序进入演示模式（可体验界面，不真实烧录）。

---

## 6. 使用场景指南

### 场景一：刻一张数据光盘

1. 插入空白 CD-R/DVD-R 光盘（`介质信息`页确认「空白：是」）
2. 「新建数据光盘项目」→ 拖入文件/文件夹
3. 检查容量条是否超限；修改卷标
4. 「开始烧录」→ 等待进度 100% + 「Fixating」完成
5. 弹出后取出光盘

### 场景二：刻一张音乐 CD

1. 准备 WAV/MP3/OGG/FLAC 文件（非 WAV 需要 ffmpeg）
2. 「新建音乐 CD 项目」→ 添加音轨（注意总时长 ≤ 介质容量）
3. 「刻录音乐 CD」→ 自动转码 → 烧录

### 场景三：把一首歌重复刻满整盘（同曲多轨）

1. 将源音频文件**复制为多份不同文件名的副本**（`cp song.wav track-01.wav` 等）
2. 全部加入音乐 CD 项目（每份一个音轨）
3. 刻录

> ⚠️ 不要用同一个文件路径重复添加：wodim 对重复路径只读取一次，
> 后续音轨会变成 4 秒空轨（详见[第 9 节](#9-已知问题与修复)）。

### 场景四：复制一张光盘

1. 源盘放入光驱 → 「复制光盘」→ 选择方式 → 开始复制
2. 数据盘：读取完成提示换空白盘 → 写入
3. 音频盘：选择「仅音频盘」使用 cdrdao 精确复制

### 场景五：擦除可重写光盘

1. 「擦除光盘」→ 确认介质可擦除（CD-RW / DVD±RW 等）
2. 选快速/完全 → 确认 → 擦除后介质恢复空白

### 场景六：迷你盘（8cm CD）

- 容量自动识别（如 219 MB / 25:02），容量条与音频时长限制按真实值计算
- 数据文件别超过 ~210 MB；音乐别超过 ~25 分钟

---

## 7. 底层工具依赖

程序启动时通过 `PATH` 搜索检测以下工具：

| 工具 | 用途 | 缺失影响 |
|------|------|----------|
| `wodim` / `cdrecord` | CD 写入、擦除、介质检测 | 无法刻 CD、无法擦除 |
| `genisoimage` / `mkisofs` | ISO 镜像制作 | 数据盘/ISO 制作不可用 |
| `growisofs` | DVD/BD 写入 | DVD/BD 烧录不可用 |
| `cdrdao` | 音频盘精确复制 | 复制音频盘不可用 |
| `dvd+rw-mediainfo` | DVD/BD 介质检测 | 介质检测信息减少 |
| `dvd+rw-format` | DVD+RW/BD-RE 格式化擦除 | 强制格式化不可用 |
| `dd` | 整盘读取（复制） | 复制数据盘不可用 |
| `ffmpeg` | 音频转码 | 非 WAV 音轨无法刻录 |
| `eject` | 弹出光盘 | 弹出按钮无效 |

**降级规则**：没有光驱或缺少 `wodim`/`growisofs` 时，程序进入**演示模式**——所有烧录操作模拟进度与日志，方便无光驱环境体验。

**权限要求**：用户需在 `cdrom` 组（Debian/Ubuntu 默认加入）才能直接写 `/dev/sr*`；否则需以 root 运行。

---

## 8. 技术架构

### 8.1 模块划分

```
src/
├── main.cpp            入口：应用设置、--check-media 自检、主窗口
├── Types.h             公共类型：JobKind、MediaStatus、格式化工具函数
├── Settings.h/.cpp     刻录设置（QSettings 持久化）
├── JobRunner.h/.cpp    作业执行器
├── Backend.h/.cpp      刻录后端
├── Projects.h/.cpp     数据项目 / 音乐项目模型
├── Icons.h             程序内绘制图标（QPainter，无资源文件）
├── MainWindow.h/.cpp   主窗口：菜单/工具栏/导航/状态栏/页面调度
└── pages/
    ├── WelcomePage.*        欢迎页卡片
    ├── DataProjectPage.*    数据项目页
    ├── AudioProjectPage.*   音乐 CD 项目页
    ├── CopyDiscPage.*       复制光盘页
    ├── IsoToolsPage.*       ISO 镜像工具页
    ├── MediaInfoPage.*      介质信息页
    ├── ErasePage.*          擦除页
    ├── BurnJobPage.*        烧录进度页
    └── BurnOptionsDialog.*  刻录设置对话框
```

### 8.2 数据流（以数据盘烧录为例）

```
DataProjectPage → Backend::burnData()
                        │
                        ├─ 1. detectMedia()：确认介质可写（同步，2~4 秒）
                        ├─ 2. 队列入队（QueuedJob）
                        │     └─ CD：genisoimage 制作临时 ISO
                        │           └─ 完成后回调：wodim -v fs=6000 ... 烧录
                        │     └─ DVD/BD：growisofs -Z ... 直接写入
                        └─ JobRunner 实时解析输出
                              ├─ logLine  → BurnJobPage 日志
                              ├─ progress → BurnJobPage 进度条
                              └─ finished → Backend 队列调度下一个作业
```

### 8.3 关键设计

- **作业队列**：一次操作可能拆成多个底层命令（复制 = 读 + 写；CD 数据盘 = 制作 ISO + 烧录）。`Backend` 用队列串行执行，`after` 回调链式衔接。
- **进度解析**：`JobRunner::parseProgress()` 按作业类型匹配正则——growisofs `( 27.2%)`、wodim `% written`、cdrdao 百分比、dd 字节数（需预设总字节数）。
- **演示模式**：`JobRunner::startDemo()` 定时器模拟 growisofs 格式的进度输出，走同一套信号链路。
- **介质检测**：三层探测（dvd+rw-mediainfo → wodim -atip → msinfo+toc），解析结果合并为 `MediaStatus`。

### 8.4 信号流

```
Backend ──logLine/progress/detailChanged/stateChanged──→ BurnJobPage
    └────mediaChanged/backendChanged──→ 状态栏、容量条、各页刷新
    └────jobFinished──→ MainWindow 状态栏消息
各操作页 ──burnRequested(title)──→ MainWindow → BurnJobPage::showJob + 跳转
```

---

## 9. 已知问题与修复

### 9.1 wodim 缓冲区 mmap 失败（重要）

**现象**：

```
/usr/bin/wodim: Operation not permitted. Warning: Cannot raise RLIMIT_MEMLOCK limits.
/usr/bin/wodim: Resource temporarily unavailable. Cannot get mmap for 12587008 Bytes on /dev/zero.
```

**原因**：cdrkit 的 wodim 默认会 `mmap` 约 12.5MB 缓冲区，普通用户的 `RLIMIT_MEMLOCK` 默认仅 8MB，映射失败导致直接退出。

**修复**：程序对所有 wodim 调用显式传 `fs=6000`（FIFO 6MB），wodim 改用普通内存分配路径。已在便携光驱 + CD-R 上实测通过。

**彻底解决**（恢复大缓冲区）：在 `/etc/security/limits.conf` 添加

```
<用户名> - memlock unlimited
```

并重新登录。

### 9.2 同一文件重复添加为音轨 → 空轨

**现象**：同一 WAV 路径添加 4 次刻录，只有第 1 轨完整，其余音轨只有 4 秒。

**原因**：wodim 对相同路径的文件句柄复用，读取完第 1 轨后指针在 EOF，后续音轨读到 0 字节。

**修复**：将源文件复制为不同路径的副本再添加（见[场景三](#场景三把一首歌重复刻满整盘同曲多轨)）。程序侧暂未自动去重（添加自动复制副本的功能列入待办）。

### 9.3 老光驱 DAO 模式拒收 CUE sheet

**现象**：`wodim: Cannot send CUE sheet.` / `Could not write Lead-in.`

**原因**：部分老光驱不支持 SAO/DAO 的 CUE sheet 命令。

**修复**：数据盘与音频盘统一使用 **TAO 模式**（音频轨道间保留标准 2 秒间隙）。已验证。

### 9.4 强制低速刻录无效（光驱锁定写速）

**现象**：设置 1x/4x 后日志仍显示 `Speed set to 1764 KB/s`（10x）。

**原因**：部分便携/老光驱忽略 SET SPEED 命令，固定以单一速度写盘。

**处理**：属光驱硬件限制。换支持低速的光驱后，程序的下拉框选择即生效。

### 9.5 便携光驱无法远程关仓

**现象**：弹出后 `eject -t` 报错，托盘无法自动关闭。

**处理**：手动推回托盘。程序「弹出」按钮只弹出。

### 9.6 连续模拟测试后光驱状态异常

**现象**：多次 dummy 烧录后，介质检测出现异常值（如 `Blocks remaining` 负数、TOC 读不出）。

**处理**：弹出 → 推回重置光驱即恢复（老光驱固件的已知脾气）。

### 9.7 无介质时误报「有 CD」

**原因**：wodim 无盘时输出 `Cannot load media with this drive!`（而非标准 `No media`），解析曾漏判。

**修复**：已加入 `Cannot load media` 检测模式（v1.0.0 修复）。

### 9.8 音频盘被误判为空白

**原因**：空白判定曾仅依赖 `-msinfo`（查 ISO 会话），音频盘无 ISO 会话被误判空白。

**修复**：增加 `-toc` 轨道检查：能列出音轨即非空白（v1.0.0 修复）。

---

## 10. 故障排查

| 现象 | 排查要点 |
|------|----------|
| 烧录失败：`Cannot get mmap` | 见 9.1，确认程序版本 ≥ v1.0.0（已含 fs=6000 修复） |
| 烧录失败：`No disk / Wrong disk!` | 光驱无介质或介质已封盘；`介质信息`页刷新确认 |
| 烧录失败：`Cannot load media` | 托盘无盘；手动推入盘片 |
| 烧录失败：写入中途 `write error` | 检查盘片质量/速度；尝试 4x 或 1x；换盘重试 |
| 烧录失败：`CUE sheet` 类错误 | 见 9.3（程序已用 TAO 规避） |
| 烧录失败：空间不足 / overburn 提示 | 文件超过介质容量；迷你盘注意 219MB 上限 |
| 进度条不动 | ISO 制作阶段为不定进度（动画）；等待即可 |
| 无法写入 /dev/sr0 | 用户不在 cdrom 组；`sudo usermod -aG cdrom <用户>` 后重新登录 |
| 音乐 CD 刻录失败：`Bad audio track size` | 音轨时长 < 4 秒（红皮书最小音轨长度） |
| 音乐 CD 刻录失败：`Cannot open` | ffmpeg 未安装且音轨非 WAV；安装 ffmpeg |
| 演示模式误开启 | 检查 `wodim`/`growisofs` 是否安装、光驱是否接入 |
| 烧录被取消后盘不可用 | TAO 中断写入的 CD-R 无法恢复；换新盘重刻 |

---

## 11. 命令行自检

```bash
./build/k3b-cn --check-media
```

输出示例：

```
== K3b 中文版 自检 ==
光驱：/dev/sr0
工具：wodim=有 genisoimage=有 growisofs=有 cdrdao=有 dvd+rw-mediainfo=有 dd=有 ffmpeg=有 eject=有
演示模式：关
介质：
  存在：是
  类型：CD-R（CD-R）
  空白：是  可写：是  可擦除：否
  容量：219 MB
  厂商：  型号：DVD RW
```

用于快速验证环境：光驱、工具链、介质状态。可配合 `QT_QPA_PLATFORM=offscreen` 在无显示环境运行。

---

## 12. 版本历史

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.0.0 | 2026-08 | 首个版本：全部功能、wodim fs=6000 修复、TAO 模式、迷你盘容量识别、无介质/音频盘检测修复、1x/4x 速度预置、GitHub Releases 打包 |

---

## 13. 常见问题 FAQ

**Q: 这个软件和 KDE 的 K3b 是什么关系？**
A: 无关。仅界面布局与操作流程借鉴，完全独立实现，代码不共享。

**Q: 为什么启动后是演示模式？**
A: 未检测到光驱或 wodim/growisofs。安装工具或接入光驱后自动恢复真实模式。

**Q: 刻好的 CD-R 还能再刻吗？**
A: 不能。CD-R 是一次性介质，封盘后不可追加、不可擦除。需要多会话刻录请事先在设置中开启。

**Q: 迷你盘容量为什么显示 219MB 而不是 700MB？**
A: 8cm 迷你盘真实容量约 219MB（25 分钟），程序按 ATIP 精确识别，避免误刻超容。

**Q: 音乐 CD 在电脑上显示为空盘？**
A: 红皮书音频 CD 没有文件系统，文件管理器不显示内容属正常；用播放器播放或抓轨工具验证。

**Q: 想用 1x/4x 慢速刻录但没效果？**
A: 见 9.4——光驱硬件锁定写速时无法强制；换支持低速的光驱即可。

**Q: 烧录中断后盘还能用吗？**
A: 已开始写入的 CD-R 中断后无法再写（染料已部分定型）。可擦写介质（CD-RW 等）可擦除重来。

**Q: 如何卸载？**
A: deb 安装：`sudo dpkg -r k3b-cn`；tar.gz 安装：删除复制的二进制与菜单项文件。

---

*文档版本：v1.0.0 · 与源码仓库同步维护*
